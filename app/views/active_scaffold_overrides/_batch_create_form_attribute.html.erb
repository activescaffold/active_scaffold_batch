<%
  scope ||= nil
  if column == batch_create_by_column.to_sym
    field = active_scaffold_batch_create_by_column column, scope, object: record
  end
%>
<%= form_attribute(column, record, scope, field: field) %>