<%
  records = if @error_records.present?
    @error_records
  else
    {scope => @record}
  end
-%>
<%= render batch_create_multiple_layout, columns: columns, form_action: form_action, records: records %>
