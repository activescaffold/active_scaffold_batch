<%
  scope_select_options = active_scaffold_update_scope_select_options
  field = active_scaffold_update_scope_select(scope_select_options)
%>
<% if active_scaffold_update_scope_select_hidden? %>
  <%= field %>
<% else %>
  <%= form_attribute_html(nil, label_tag('record_batch_scope', as_(:records)), field, nil, {}) %>
<% end %>